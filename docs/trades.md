# docs/trades.md — Trade engine, Pending Trades & Trade Lab

> Loaded on demand from CLAUDE.md. Read when editing `fantasy/trades.py`, `trade_lab.py`, or the trade sections of `send_digest.py`. Rules here are as authoritative as CLAUDE.md.

**`trade_lab.py`** reads the same snapshot and builds an **interactive, browser-only "Trade Lab"** — `previews/tradelab_{team_slug}.html`, a three-column `My Team | Trade | Trade Partner` builder: pick any two teams, see each roster grouped by role (Hitters / SP / RP), click players to assemble a deal, watch it graded live (value tilt, verdict, categories/positions gained-lost, timing, partner-fit). **The ONLY tool in the repo that uses JavaScript** — so it is **NOT emailable** (no `--email` flag). Like `dashboard.py` it **imports `send_digest` and reuses its scoring/trade functions verbatim**. A header **freshness badge** (`_freshness`, from the snapshot's `refreshed_at`) shows the refresh time in ET with a green/yellow/red dot (today / 1-day / 2+-day stale → run `--refresh`). **The digest deep-links here:** every Pending Trades / Trade Radar card carries a "Build in Trade Lab" button (`send_digest._tradelab_button`, `TRADE_LAB_URL` constant = the hosted pocket URL below) pointing at `#partner=&give=&get=`, which `preloadFromHash()` (below) parses on load.
- **Pocket mode (phone, one-tap refresh) — GitHub Pages + Cloudflare Worker** (design → NOTES; setup → `worker/README.md`; live URL + token gotcha → [[pocket-tradelab]] memory). **`main()` flags:** `--out PATH` writes the HTML + a sibling **`build.json`** (`{built_at, refreshed_at}`) freshness marker (CI publishes `public/index.html`); `--refresh-url URL` (or env `POCKET_REFRESH_URL`) bakes a **↻ Refresh data** button into the header (`build_html` renders it ONLY when `data.get("refreshUrl")` — absent on normal local runs). `refreshUrl` + a per-render `builtAt` are baked into `DATA`. **Refresh flow:** button → `doRefresh()` POSTs to a **Cloudflare Worker** (`worker/worker.js`, the ONLY holder of a fine-grained `Actions:write` GitHub token) → Worker fires `repository_dispatch(refresh-tradelab)` → **`.github/workflows/pocket-tradelab.yml`** (workflow_dispatch + repository_dispatch, **no schedule**) runs `trade_lab.py --refresh --out public/index.html` (env `POCKET_REFRESH_URL` = a repo Actions **variable**, not a secret) → deploys via Pages. The page's `checkBuild()` polls `build.json?t=…` every 15s (≤6 min) and reloads when `built_at != DATA.builtAt`. **Pocket UI (additive `@media (max-width:640px)`, desktop untouched):** single column, big tap targets, sticky `.sidehead`, a fixed-bottom **`#dealbar`** (`display:none` on desktop) filled by `setDealBar()` (Give N · Get N · net · verdict), tap (`jumpToDeal`) scrolls to `#mid`. **Opens fully folded on the HOSTED build (any device):** `pocketCollapseDefaults()` (in the bootstrap IIFE, run BEFORE the first render) pre-seeds `collapsed`/`collapsedPos` for both sides (every role section + hitter position sub-group) and closes the `#fitboard` `<details>` — so the hosted page shows a short list of expandable headers, not a wall of players, on phone OR desktop. **Gated on `DATA.refreshUrl`** (baked ONLY into the `--refresh-url` pocket/Pages build), NOT the viewport — so local `python trade_lab.py` desktop dev runs stay fully expanded (no `refreshUrl` → the check returns early). Taps still toggle live; a preloaded deal still grades in the center even with its rows folded.
- **Server side (`build_data(snap, my_team)`):** calls the SAME shared prelude helpers as `build_email`/`dashboard.build_context` (`sd.prepare_scoring` + `sd.build_recent_indexes` — so `hit_pctile`/`pit_pctile`/`_tval` match the digest exactly). For **every** team it enriches each YEAR roster row via **`sd._enrich_trade_player`** (attaches `_tscore`/`_tcats`/`_tval`/`_tsell`/`_tbuy`/`_tgroups`) and `_serialize`s a JSON-safe record per player: name, MLB logo, role (`hit`/`sp`/`rp` via `_is_sp`), stat line, score, pre-rendered badges HTML (`hitter_badges` / **`sp_skill_badges`+**`blowup_badge`+`pitcher_regression_badge` / `pitcher_regression_badge`), pre-rendered score-breakdown prose (`_hitter_score_breakdown` / `_pitcher_score_breakdown` **+`_sp_skill_context` for sp**), `tval`/`tcats`/`tgroups`/`sell`/`buy`. **`sp_skill_badges` (in `fantasy/scoring.py`; `_sp_skill_context` "why" in `fantasy/analytics.py`) is the SEASON analog of the digest's per-start QS/5K+ chips: a cyan **QS** when the arm's matchup-neutralized season QS% clears `_QS_SEASON_MIN`(55) and a yellow **K+** when his season K rate clears `_K_SEASON_MIN`(0.26) — both gated on `_SP_SKILL_MIN_IP`(30) so a hot small sample can't false-fire. Shown on **every trade surface** (Trade Lab + the digest Trade Radar / Pending Trades cards + the dashboard tile — see `_trade_skill_badges` below), but deliberately kept OFF the digest's My Upcoming Starts / FA SP (which carry the per-start `qs_badge`/`k5_badge`), so the two QS meanings never collide.** Per-team meta = `needs`/`surplus` cats + `need_pos`/`surplus_pos`, all built via the **overlay-aware `sd._team_trade_context(tk, ranks, n, pos_data)`** (per team) — the SAME source `find_trades` generates on, so the live JS verdict (`netMe`/`needMult`) grades against the exact needs the deal was built for (punt SVHD, target C/SS folded into `need_pos`). Without this the builder re-derived raw rank-thirds and could DECLINE a board-recommended deal. `need_pos` is serialized as `{pos: my_avg_score}` for the JS upgrade gate. All → one `json.dumps` blob (`</` escaped for `</script>` safety).
- **Client side (embedded JS):** renders both rosters from the blob (score pills color-coded like `_mini_badge`; clicking a pill toggles its breakdown; clicking a row toggles the player into that side's package). The center panel recomputes live: base `netVal = Σtval(get) − Σtval(give)`, plus **demand-side `netMe`/`netThem`** (each side re-values by its own needs via `needMult`/`sumEff`, JS mirrors of `_need_mult`), `timing` = sell-out + buy-in − buy-out − sell-in, category gain/lose chips (green = fills a LEFT-team need, red = loses a non-surplus strength), positional upgrades. **CALM two-rail progressive-disclosure layout (`#cols` = `3fr 4fr 3fr`, middle wider):** the resting `#mid` = verdict pill · a give/get **ledger** where each `ledgerItem` renders the player's `tval` + an **arb-marker echo** (amber ▸ / green ◂, see roster layout) + a per-rail **subtotal** (`#giveSub`/`#getSub`), the name/value **tappable (`openVs`) to a per-player value story** — `valueStory(p)`: base drivers (`tcats`/`tgroups`) then the SAME player re-priced `To you` / `To <partner>` via `needMult`, with ± reasons from **`multParts`** (a faithful client mirror of `_need_mult`'s branches, so the story can't drift from the value math) · a **fairness bar** (`#fairbar`: a give↔green-get split sized by BASE value + a one-line tilt caption) · the `dealSummary(...)` **takeaway** (`#dealsum`) · then `#reads` = ONLY the two acceptance lines. **Everything numeric moves behind a collapsed `<details id="vdetail">` ("Value detail")**: the **three-row Base / My value / Their value give-get-net matrix** (`_vrow`) + the category gain/lose chip lines. **Column convention: every row's Net = Get − Give** (matching the "Give"/"Get" headers literally). The "Their value" row therefore passes `_vrow('Their value', thGet, thGive, netThem, ...)` — `thGet`/`thGive` swapped relative to their variable names (which are MY give/get value re-priced by the partner) so the DISPLAYED Give/Get columns read as *their* give/get and `Get − Give` still lands on `netThem` (`= thGive − thGet`, "+ = they win"). Passing `thGive, thGet` in that order (the naive reading) silently breaks the Get−Give pattern the two rows above establish — don't revert it. `dealSummary(netVal, netMe, netThem, addressesNeed)` = a base clause off `netVal` (roster-blind raw value) + a tail off the two demand-side nets (e.g. "A slight overpay on paper, but it fills both sides' needs — a win-win"). The **verdict** mirrors `_pending_verdict` keyed on `netMe` (`trap = timing<0` → ACCEPT/COUNTER/DECLINE), downgrading an ACCEPT to COUNTER on a `dealStarSurrender` OR a **my-side depth short** (`leavesShort(myMeta.pos_count, giveArr, getArr)`); **"Would they do it?"** mirrors `_trade_tilt(net_them)` — AGGRESSIVE (`netThem ≥ -realisticMax`) + graduated `dealStarReach`, but **hard-balks first when `leavesShort(partnerMeta.pos_count, getArr, giveArr)`** (they won't gut a position) and applies a read-only `netThemRead` penalty (`DATA.tune.thinPosPenalty` × `thinPos` single-slot count) so a thin loss flips the read (`pfTier` no/maybe/yes/na). **OVERALL-DEAL check (the pill can't contradict "Would they do it?" below it):** an otherwise-ACCEPT is downgraded to COUNTER whenever `pfTier !== 'yes' && pfTier !== 'na'` — i.e. a deal that's good for ME but that the rival would balk at (star reach, a depth gut, or a bad-for-them net) isn't landable as ACCEPT no matter how good it looks on my side; `why` becomes "good for you, but " + the partner-side reason. **"Would you do it?"** uses `dealStarSurrender`, balks on a my-side short, AND now checks `trap` too (a need-filling deal with bad timing reads `maybe`, not `yes`) — the same trap gate the pill above already uses, so this line can no longer disagree with it. `leavesShort`/`thinPos` are the JS mirrors of `_leaves_position_short` (body-count depth floor, hitter slots only, keyed off per-team `pos_count` + `DATA.posStarters`) — value-display rows stay pure, the penalty is read-layer only. **Both reads render with a sentiment marker** (`accLine`/`ACC`: 👍 green likes / 🤔 yellow stretch / 👎 red balks) + short reason. All tuning (`DATA.tune`: star curve + realistic band + need-mult constants + `thinPosPenalty`) is baked from the send_digest globals so the Lab can't drift. LEFT defaults to my team (or `--team`); either dropdown can pick any of the 12 teams.
- **Roster layout (`renderRoster` + `groupHitters`/`playerRowHtml`/`gaugeHtml`/`toggleSection`):** three role sections (Hitters / SP / RP). **Hitters are grouped under EVERY eligible position** (`groupHitters` buckets a bat into each `POS_GROUPS` slot in `posTokens`, so a 2B/SS/OF bat appears under all three — a per-slot "how strong here?" scan; SP/RP stay flat). A multi-eligible bat therefore has multiple DOM rows — `playerRowHtml` gives each a `gkey`-unique id but tags it `data-pid`, and **`toggle(side,id)` syncs `.sel` across EVERY copy** by `data-pid` (selection registers once in `picked`). A hitter with no recognized slot lands in a **UTIL** bucket (nobody dropped). **Both role sections AND hitter position sub-groups are collapsible** — the `.rolehdr` calls `toggleSection(side,role)` (fold state in the `collapsed` global, side→role→bool) and each `.possubhdr` calls `togglePos(side,pos)` (the `collapsedPos` global, side→pos→bool); both persist across re-renders (caret ▼/▶, `.secbody`/`.possecbody` `display:none`). **Rank tag (`rankText`) — plain COLORED TEXT, NOT a pill** (a pill read like a player's score badge → confusing): each header shows a right-aligned `#N` = the RENDERED team's **league rank** (1 = best), so a FOLDED section says "worth digging / trading for?" at a glance. Three sources: **hitter position sub-headers** → `teamsMeta[tk].pos_rank[pos]` (per-slot); **SP/RP role headers** → `pos_rank['SP']`/`['RP']` (crew rank); **Hitters role header** → `teamsMeta[tk].hit_rank`, the team's **AGGREGATE lineup-hitting rank** (so a folded Hitters section says "Xth-best hitters in the league"). `pos_rank` is built server-side in `build_data` from `sd.positional_breakdown` (C/1B/2B/3B/SS/OF/SP/RP); `hit_rank` ranks teams by Σ per-position `my_avg × POS_STARTERS` (same starter-avg values shown per position). Color: top-third GREEN / middle YELLOW / bottom-third RED; tooltip = plain-English `rankPhrase` ("2nd-best of 12" / "2nd-worst of 12", via `ordinal`). Unranked buckets (UTIL) show no tag. `openBd(bid)` takes the row's unique breakdown-element id.
- **Player rows** show a compact **position chip** after the name (hitters only — `_pos_tokens` collapses OF variants; pitchers already grouped SP/RP), and **partner-side players get a 🎯 target marker** when they'd help MY (left) team — via `targetReasons(p, meta, poss)` (strong in one of my need cats, or a hitter upgrading a thin `need_pos`). `targetReasons` is reused from BOTH perspectives; the `poss` arg flips "your"↔"their". A 🎯 target's row edge is a **soft-WHITE** hint (`.prow.target`) — **GREEN is reserved for the arb marker**, so a green edge always means "value edge", never merely "fills a need".
- **Value-asymmetry ("arb") marker — a SEPARATE signal that lives ALONGSIDE 🎯, on BOTH rosters + the center ledger.** Answers "is there a value EDGE?" (does one side price him higher than his current owner?), distinct from 🎯 ("does he fill my need?") and from the `$`/`▼` Statcast luck badges (a different axis). Computed client-side: `arbMarker(p, holderMeta, otherMeta)` on the **gap** `needMult(other) − needMult(holder)`; fires when `gap ≥ DATA.tune.needCat` (glyph), doubled when `gap ≥ needCat + needSurplus` (strong). Direction points **inward toward the trade**: LEFT roster (holder = my team) → **amber ▸/▸▸ "send"** (my surplus the partner values more; `.arb.bait`, optional right-edge tint `.prow.abait`); RIGHT roster (holder = partner) → **green ◂/◂◂ "grab"** (their surplus I value more; `.arb.grab` + green edge `.prow.agrab`, which **wins over the soft-white target edge** when a player is both). No arb = a keeper. `renderRoster` passes the OTHER dropdown's meta as `otherMeta`; both dropdowns already re-render both rosters (`initSide`) so markers recompute on either team change. Glyphs are **HTML entities** (`&#9656;`/`&#9666;`) not `\uXXXX` — `_JS` is a raw string so `\u` would survive literally. `arbReasons`/`multParts` build the hover tooltip + value-story reasons. **Display/read-layer only — never touches `_tval`, `needMult`'s use in the verdict, or any score.**
- **Deal Coach (`renderCoach`, center column):** match-up context (You need / They need / Your leverage = my surplus cats ∩ their need cats) + two value-ranked **clickable** suggestion lists (**Add to get** = partner players filling my needs; **Offer them** = my players filling theirs; each click `toggle`s the player) + a live **balance nudge** off the running `net`. A **`Fair | Favor me | Fleece` strategy toggle** (`strategy` global; `TARGET_NET`/`STUD_CEIL`) reshapes the COACH ONLY (never the objective verdict): moving right protects my studs from the Offer list (value ceiling), leads with cheaper need-fillers (ascending `tval`), steers the nudge toward a bigger edge. **A COUNTER driven by overpaying NAMES the specific partner add-on to request** (`counterAddon` — mirrors `_counter_suggestion`): a spare partner piece worth `[0.7·gap, gap+0.45]`, preferring one that fills a need of mine (then buy-low, then closest to the gap), rendered as a **clickable** `.counteradd` name that `toggle`s it into GET. The center Trade column is **wider than the two team columns** (`#cols` grid `3fr 4fr 3fr`).
- **Partner Fit board ("Who should you be trading with?", `#fitboard` — a collapsible `<details open>` ABOVE `#cols`):** ranks EVERY rival by how landable a deal is, showing one concrete engine-graded deal each in plain English (no raw `tval`). **Uses the real trade engine SERVER-SIDE** (a contained departure from the Lab's "JS only sums" rule, like `dashboard.py` — see NOTES). `build_partner_fit(pitchers, hitters, roto, team_keys, ranks, n, best_recent_p, best_recent_h, hit_pctile, pit_pctile, pos_data_by_team)` (end of `build_data`, reusing per-team `pos_data` stashed in `pos_data_by_team`) runs `sd.find_trades_combined` **from EVERY team's POV** with caps temporarily raised (`sd._TRADE_MAX_CARDS`/`_TRADE_PER_TEAM_CAP` → 400/6 in a `try/finally`). **Candidates are first filtered to `my_verdict == "ACCEPT"` deals** (good for MY side) — a deal that helps the rival but not me is exactly what made the board headline a deal the builder then DECLINEd, so it's dropped to a diagnosis instead. Per rival it then picks the single best surviving deal (realistic-first via `sd._trade_tilt` passing `net_them`, then scarce need C/SS/SVHD, then get-value) and assigns a tier: **BEST** — a true **win-win**: realistic price AND the rival **clearly gains** (`net_them ≥ _TRADE_RIVAL_GAIN_MIN`); **REACH=WORTH A SHOT** — good for me but the rival wins little/nothing (aggressive ask); / a diagnosis for no-ACCEPT-deal rivals from category intersections (`i_offer = my_surplus ∩ their_needs`, `they_offer = their_surplus ∩ my_needs`, both via the overlay-aware `_team_trade_context` pov needs) → **NOFIT=NO DEAL** (both empty), **ONEWAY** (I'd overpay), **SLIM** (overlap but no near-even ACCEPT-for-me deal). Phrasing via `_fit_deal_words(value_phrase, accept, rival_gains)` (BEST gated on `rival_gains`); need tags via `_fit_get_tags`. **`whyOffer` names only the cats the players I'd ACTUALLY send cover for them (the deal's own `send_cats`), NOT the roster-wide `my_surplus ∩ their_needs` intersection** — so the "you're deep in X (their needs)" prose can't overclaim a strength the give players don't carry. Serialized as `DATA.partnerFit = {pov_key: [records...]}`; a record is ACTIONABLE (`tier`, `get`, `give`, `verdict`, `whyOffer`, `whyGet`) or a DIAGNOSIS (`tier`, `why`). **Client:** `renderFitBoard()` reads `DATA.partnerFit[selL.value]` + fills the `#fbneeds` needs summary from `teamsMeta[myTk]`; re-rendered on LEFT-team change + bootstrap. Each card's **"Build this ▶"** (`loadDeal(el)`, reading `data-partner`/`data-give`/`data-get`) `clearAll()`s, sets `selR`, selects via `pickByName`, **folds the `#fitboard` `<details>` shut** (so the builder isn't buried under the board), and `scrollIntoView`s the builder. Tier colors in `FIT_TIER`; CSS scoped under `#fitboard`/`.fb*`.
- **💫 Blockbuster strip (`build_megadeal_board` → `DATA.megaDeals` → `#megawrap`/`#megalist`, ABOVE the per-rival board):** the multi-player win-win the base engine can't form (give roster depth, get fewer-but-better — a scarce C/SS upgrade). **User-facing label is "Blockbuster"** (header "💫 Blockbuster deals"); the internal name stays "consolidation megadeal"/`find_megadeals`/`_MEGA_*`. `build_megadeal_board(pitchers, hitters, roto, team_keys, ranks, n, …, pos_data_by_team)` runs `sd.find_megadeals(pov, limit=2)` (see the megadeal shape in Trade Radar) from **every** POV so the strip stays correct when the LEFT dropdown switches; each record MIRRORS the Partner-Fit ACTIONABLE shape (`{team, get:[{name,tags}], give:[name], verdict, whyOffer, whyGet}`) with **`tier:"MEGA"`** PLUS a dynamic **`spark`** string ("why it's a blockbuster" — how many depth pieces roll into how many difference-makers, needs fixed, and that they win too), so the client renders it with the SAME `fitCard` markup + "Build this" (`loadDeal` already handles N players). `FIT_TIER.MEGA = [PURPLE, "💫 BLOCKBUSTER"]`; `fitCard`'s actionable branch + green verdict color extend to `MEGA`, adds a purple `.fbspark` callout and a `.fbmega` purple card highlight; `renderFitBoard` fills `#megalist` from `DATA.megaDeals[selL.value]` and shows `#megawrap` only when non-empty. Win-win only (some days empty → strip hidden). CSS `.megahdr`/`.megasub`/`.megalist`/`.fbspark`/`.fbcard.fbmega` scoped under the fitboard (PURPLE per user preference).
  - **LIVE-BUILDER blockbuster indicator (`recompute`, center panel):** when the *hand-built* deal in the builder is itself a genuine blockbuster — **give ≥ get, give side ≥ 3** (mirrors the mega shapes) AND it grades a two-sided win (`label==='ACCEPT'` AND partner-fit `pfTier==='yes'`) — the center `#mid` panel gets a purple glow (`.midmega`) and a purple `.megaline` banner under the verdict ("💫 **Blockbuster!** Roll N depth pieces into M difference-makers — fixes K needs in one move, and they still win too."). So building your own multi-player consolidation feels like the headline it is; the class is toggled off on any non-blockbuster or empty deal. **This is why a hand-built 4-for-3 the engine won't auto-generate still celebrates here:** the Trade Radar only ASSEMBLES deals from pieces that map to a measured need on each side (a rival category hole to give into, my category/position hole to get), so a real deal that includes pieces swapped for roster-feel reasons the rank model can't see (a starter I'm already surplus in, a bat that doesn't plug the rival's category hole) never generates — but the grader still confirms it ACCEPT/win-win when you build it, and the builder now flags it.

### Trade Radar (`find_trades` + `build_trade_radar`) — cross-team, FAVOR-ME
Surfaces trade ideas that **fix a rival's category need** (their reason to accept). Two lanes (see TWO LANES): a **FAIR** lane (realistic, near-even/pay-up deals a rival would accept — the ones that land a scarce C/SS upgrade) and a **FAVOR-ME** lane (good-not-great pieces, my elite bats protected, value tilted to me, buy-low/sell-high timing); `find_trades_combined` blends them, leading with fair. Perspective-driven via `my_team`, so it works under `--team` for free — **not gated** on `not override_team`. Renders in the TRANSACTIONS band (anchor id stays `band-fa`) after FA Hitters (`build_email` body_parts, section 13b); returns `""` when no candidates exist. **The digest shows up to 3 normal (≤2-per-side) cards + 1 💫 BLOCKBUSTER megadeal** (`find_megadeals(limit=1)`, appended last as the HEADLINE: a solid PURPLE→MAGENTA "💫 Blockbuster" chip, a brighter purple card border + faint purple wash, and a dynamic "💫 Why it's a blockbuster" callout that sells the excitement; plus a purple subtitle note; see the megadeal shape above) — `build_trade_radar` passes `cards=3` and renders `all_deals = trades + mega`.
- **`realistic_only` — a GENERATION-TIME WIN-WIN gate, not just a display label.** `find_trades(..., realistic_only=False)` / `find_trades_combined(..., realistic_only=False)`: when `True`, a candidate package is dropped (`continue`, right after `timing`/`my_verdict` are finalized) unless it is a genuine win-win for BOTH sides — **(a)** it grades **ACCEPT for me** (`my_verdict == "ACCEPT"`, the SAME `_pending_verdict(net_me, addresses_need, timing, incoming=True)` the Trade Lab / pending-offer surfaces show), **(b)** the rival **clearly gains** (`net_them ≥ _TRADE_RIVAL_GAIN_MIN`(0.05) — a real demand-side gain, not merely break-even), and **(c)** I'm not prying their star at par (`not _deal_star_reach`). This is the **cohesion keystone**: because generation validates against the exact verdict the builder shows, the Trade Radar can no longer recommend a deal the Trade Lab then DECLINEs (the old gate checked only the rival's `_trade_tilt == "realistic"` band and never asked whether the deal helped ME — that's what let a value-losing SP-for-SP deal read "BEST" and then "DECLINE"). `build_trade_radar` (digest) and dashboard `render_trade_radar`/`build_context` pass `realistic_only=True`; some days a rival may surface zero cards, which is intended. Trade Lab's Partner Fit Board (`build_partner_fit`) leaves it `False` (default) but achieves the same cohesion a different way — it filters its candidate pool to `my_verdict == "ACCEPT"` deals before tiering (see below). **`my_verdict` is stored on every trade dict** (computed unconditionally in `find_trades`) so the board can tier on it.
- **Category matrix:** `team_category_ranks(roto)` generalizes `category_ranks` to `{team_key: {cat: rank}}` (rank 1 = best; same `{cat}_Points` aggregation, so `_LOWER_BETTER` is baked in). A team's **need cats** = bottom-third (`rank ≥ n − third + 1`), **surplus cats** = top-third (`rank ≤ third`), `third = round(n/3)`.
- **Acceptance gate:** for rival T, `send_cats = my_surplus ∩ T_needs` (cats I help them in) and `get_cats = T_surplus ∩ my_needs`. **`send_cats` must be non-empty** (their reason to say yes) or the pair is skipped; the get side can be a category need OR a positional need.
- **PERSONAL STRATEGY OVERLAY (`_TEAM_PUNT_CATS` / `_TEAM_TARGET_POS`, keyed by canonical team name) — via the ONE shared `_team_trade_context`:** the rank-derived needs are strategy-blind — they don't know a manager is *punting* a category (so chasing it is noise) or *wants* a hitter position the ranks don't flag as a deficit. **`_team_trade_context(team_key, ranks, n, pos_data)` → `(needs, surplus, need_pos, surplus_pos)`** is THE single source that applies both overlays for a given key, and it is called by **generation (`find_trades`) AND every verdict path (`_grade_pending_trades`, `trade_lab.build_data`'s `teams_meta`)** — so a deal built around a target is graded against the exact same needs (this is what killed the surface-disagreement: previously the overlay lived only in `find_trades`, so the pending/Trade Lab verdict re-derived raw rank-thirds and DECLINEd deals the radar generated). A team with no overlay entry falls through to default rank-thirds, so another manager's `--team` read is never distorted. **PUNT** = cats subtracted from `needs` (`Guerrero Warfare: {SVHD}` — I punt saves, so the radar stops flooding closer cards AND the verdict stops crediting an incoming closer as "fills a need"); **TARGET** = **HITTER** positions folded into `need_pos` even when NOT bottom-third (`Guerrero Warfare: {C, SS}`). A target still has to clear my starter avg there (the same upgrade gate as a real `need_pos`), so it surfaces a genuine upgrade, never a phantom need. **SP was intentionally REMOVED from TARGET** — a rotation-stability SP knowingly pays value for cats I already win (K/QS/W), and `_need_mult` skips the positional term for pitchers, so an SP target adds ZERO demand-side value → the verdict can only read such a deal as DECLINE. Keeping it as a target is what generated the "recommend then decline" contradiction; targets are now hitter-only so the overlay flows cleanly through `net_me`/`net_them`. **Depth-cap caveat:** a target position where I'm already at the redundancy cap of *playable* bodies (`POS_STARTERS[P] + _POS_DEPTH_SLACK`) still gets its positional credit STRIPPED by `_non_redundant_get_pos` unless I shed a same-position body. **IL-slot bodies no longer count toward that cap** (`_team_position_counts` excludes `_on_il`), so an *apparent* 2-catcher stack that is really 1 active + 1 injured surfaces a C upgrade normally. **A two-genuinely-healthy stack at a target is now handled by the UPGRADE-SWAP shape** (give my weakest body at the target + a rival-needed sweetener → get a clear upgrade) — see the package shapes below; the old "not yet built" caveat is closed.
- **CROSS-ROLE TRADE VALUE (`_trade_value` → `_tval`) — the parity currency, NOT the role-score badge** (why closers ≠ hitters at equal role-score → NOTES, "James Wood" bug). `_trade_value` = `Σ max(0, pctile(c) − 0.5) × w` over the player's role cats, `w = _TRADE_SVHD_W`(0.35) for SV+H else 1.0. Percentiles are vs the WHOLE qualified pool (hitters `hit_pctile`; pitchers `pit_pctile` = `build_cat_percentiles(all YEAR pitchers, _FA_RP_CATS)`, built in the prelude next to `hit_pctile`), so a reliever's low K/W volume ranks low and an everyday smasher outweighs a one-category closer. **The role-score badge (`_tscore`) is still DISPLAYED; parity/labels use `_tval`.**
  - **INJURY DISCOUNT (`_health_value_mult` → `_IL_TVAL_MULT`):** an IL/injured player trades for LESS but never 0 — he returns. `_enrich_trade_player` multiplies `_tval` by a severity-tiered factor keyed on ESPN's status vocabulary (`ESPN_Status` first, `FreeAgentInjuryStatus` fallback): `SIXTY_DAY_DL`/`OUT` ×0.45 (severe / season-lost), `FIFTEEN/TEN_DAY_DL`/`IL` ×0.82–0.85 (short-term — a week or two missed, small haircut), `DAY_TO_DAY` ×0.95, healthy ×1.0; a player in an IL lineup slot (`_on_il`) whose status is blank/stale-`ACTIVE` takes `_IL_TVAL_DEFAULT` (0.82, i.e. treated as short-term). **The short tiers were softened** (10-day was ×0.62 — a ~40% haircut on a 2-week absence was overkill; now ~15%). Applied in `_enrich_trade_player` **AFTER** `_trade_value`, NOT inside it, so `compute_position_scarcity`'s league starter-baseline stays on healthy value. **Symmetric across both POVs** (shared `_tval`): an injured stud a rival ships is worth less to them, and one I'd acquire off the IL is a discounted buy-low to me (e.g. Bobby Witt Jr. on the 10-day IL: raw 1.44 → 1.22, so acquiring him reads "you pay a bit" not a phantom steal). Auto-mirrored into Trade Lab (the serialized `.tval` already carries the discount — no JS change).
    - **STAR STATUS vs VALUE — `_tval_star` (`_il_strips_star` → `_IL_STAR_STRIP`):** the injury discount cuts a player's *value*, but a short-term-injured superstar is still a **star a rival won't ship at par** — a 10-day IL shouldn't turn a franchise SS into a freebie. So the star-reach premium (`_star_premium` in `_deal_star_reach`/`_deal_star_surrender`) is computed off **`_tval_star`**, NOT `_tval`: healthy (pre-injury) value for a short-term injury, discounted value ONLY for a **severe** tier (`_IL_STAR_STRIP = {SIXTY_DAY_DL, OUT}`) where a season-lost player genuinely *does* become pry-able. Set in `_enrich_trade_player` (`_tval_star = _tval if _il_strips_star(r) else _raw_val`); mirrored into Trade Lab as `tvalStar` in `_serialize` (the two JS reach fns read `p.tvalStar`). **This is what keeps "give Riley Greene + Jake McCarthy → get Bobby Witt Jr. + Drake Baldwin" out of the radar:** Witt's 10-day IL softens his *value* (so the base net even tilts my way) but he keeps his healthy star premium (0.48), so the deal correctly reads **aggressive ask** ("they won't ship their star") and drops from the prescriptive surfaces — the radar instead surfaces a sensible 1-for-1 (Greene → Witt) where I pay the small reach premium. **DISPLAY BADGE (`_il_badge` → `_IL_BADGE`):** because the `_tval` discount is otherwise invisible on a card (a manager can't see *why* Witt is valued at 0.89), every trade-card player line carries a small injury chip whose severity label mirrors the discount tiers — `IL-60`/`OUT` (RED), `IL-15`, `IL-10`, generic `IL`, `DTD` (ORANGE). Prepended to the digest card chips (`_trade_player_line`, most-salient first, right after the score pill) and appended to the Trade Lab `badges` string in `_serialize` (renders in both the pool + picked lists, zero JS change). Reads `ESPN_Status` → `FreeAgentInjuryStatus`; an IL-slot player with a stale/blank status (e.g. José Ramírez, `OnIL=True` but `ESPN_Status=ACTIVE`) still gets a generic `IL` chip via the `_on_il` fallback, so the badge and the discount can't disagree.
  - **POSITIONAL SCARCITY (`compute_position_scarcity` → `_POS_SCARCITY`, HITTERS ONLY):** the raw percentile sum is position-blind, so it scales a hitter's `_tval` by `mult[P] = baseline / starter_avg_tval[P]`, where `starter_avg_tval[P]` = mean raw `_tval` of the league's top-`POS_STARTERS[P]·n_teams` eligible bats at P, and `baseline` = the mean of those per-position averages. Weak-starter positions (e.g. **C**) → `mult > 1` (SCARCE); deep positions (e.g. **1B**) → `mult < 1`. Clamped to `_POS_SCARCITY_CLAMP`(0.75–1.50). A **multi-eligible** bat takes his **scarcest** position's multiplier. Set by `prepare_scoring` (which calls `compute_position_scarcity(hitters, hit_pctile)` right after building `hit_pctile`/`pit_pctile`) — all three trade surfaces (`build_email`, `dashboard.build_context`, `trade_lab.build_data`) go through it. **Reset to `{}` at the top of the compute fn** so the internal `_trade_value` calls return RAW value while the scale is being derived (empty global = no-op passthrough = cold-start behavior). **Pitchers are NOT scaled** — pitcher value is already punt-saves-shaped, a separate axis. Typical: C ~×1.28, 1B ~×0.78, 2B/3B/SS/OF ~×1.0.
- **Outgoing pool (GOOD-not-GREAT, my studs protected)** = my players strong in a `send_cats` cat, **at a surplus POSITION** (`pos_data` rank top-third, no hole opened), **AND `_tval ≤ _TRADE_MAX_VAL`(1.35) UNLESS `_tsell`** — elite keepers never offered, except a sell-high regression candidate. **Incoming pool** = T's players that either help a `get_cats` cat OR **`_fills_need_pos`** — a HITTER at one of my THIN/targeted hitter positions (`need_pos`) whose `_tscore` clears my positional avg there. Positional need is hitter-only (pitching need stays category-shaped — an SP is never credited a positional need). "Strong in" reuses `player_cat_strengths`. **QS/B_SO aren't per-player stats**, so a team category-need in those finds no player — intentional.
- **PACKAGE SHAPES (4):** **1-for-1** and **2-for-2** (each incomer covers ≥ 2 distinct needs) are the base shapes. Two more shapes land the scarce pieces a hand-built deal finds but the base shapes miss:
  - **2-for-1** — two of my surplus pieces for ONE rival need-filler, so value can ADD UP to acquire a C/SS stud without a single-piece overpay tripping the fair-lane pay-up cap.
  - **UPGRADE-SWAP** — give my **weakest body at a target/need hitter position P** (which the surplus-only give pool can never offer, since P is a *need*, not a surplus) PLUS a rival-needed surplus **sweetener**, to GET a clear upgrade at P (`incoming._tscore > weak._tscore`). The swap keeps my body count at P (shed 1 + add 1), so the both-parties depth floor holds and `_non_redundant_get_pos` still credits P (a swap counts, `shed>0`). This is what surfaces a genuine C/SS upgrade when I already roster two healthy bodies there (the case the old radar couldn't form). All four base shapes flow through the identical value gate + the win-win `realistic_only` gate, so a new shape can only surface as a genuine win-win.
  - **THE SHARED GRADER (`_grade_package(outs, ins, ctx)`):** every candidate package — base OR mega — is graded by ONE module-level function (extracted verbatim from `find_trades`' old inline loop, byte-identical). It applies the HARD gates (no mutual benefit; either team dropped below startable hitter depth via `_leaves_position_short`; `_deal_star_surrender`) and returns the full trade dict (`net_val`/`net_me`/`net_them`/`my_verdict`/`timing`/`score`/…) or `None`. The SOFT filter (`realistic_only` for the prescriptive surfaces, the win-win gate for megadeals) is applied by the caller on the returned dict, so a caller tunes landability without re-deriving any value math. `ctx` bundles the per-run + per-rival context (`ranks`/needs/surplus/`need_pos`/pos-counts/`mode`). (Its three `get_cats`/`get_pos`/`send_cats` sorts carry a **cat/pos-name secondary key** so equal-rank ties resolve deterministically — a bare set order flipped run-to-run and made `render_diff` false-diff.)
  - **CONSOLIDATION MEGADEALS (`find_trades(mega=True, mega_only=True)` → `find_megadeals`) — the hand-built multi-player win-win the base shapes can't form.** Base packages cap at 2/side and grade on RAW `_tval`, so a "give several depth pieces, get fewer-but-better need-fillers" deal always reads as a raw overpay and never generates — yet it's exactly what a manager builds by hand (give 4, get 3: upgrade C, add SS, consolidate SP depth). The mega path adds N-for-M shapes `{(3,2),(3,3),(4,2),(4,3),(4,4)}` — rule **`|give| ≥ |get|`** (consolidation OR even-body, up to 4/side, one side ≥ 3; give-fewer-than-get is excluded — taking on more bodies than you ship forces a roster drop, not a clean win-win) and a **relaxed raw pay-up band** `_mega_gate` (`give − get ≤ _MEGA_MAX_PAYUP`=0.90 vs fair's 0.35 — consolidation inherently overpays on raw value; steal ceiling stays `_TRADE_MAX_EDGE`).
    - **The two pools MIRROR each other — each is a CORE (that justifies the deal) + BALLAST (that balances value).** This is the insight that makes a real 4-for-3 form (diagnosed against the user's own executed deal — give Martinez/Alvarez/Drohan/Marsh → get Langeliers(C)/Edwards(SS)/**Gausman**; the grader ACCEPTs it at `net_me +0.12 / net_them +1.00`, so the block was never valuation — it was GENERATION):
      - **GIVE pool** = a **helper core** (`mega_help`: disposable pieces whose `_tcats` plug a rival category `send_cats`, capped `_MEGA_GIVE_WIDTH`=8 — the grader's `scov` requires ≥1, so this must be present) **+ DEPTH ballast** (`mega_depth`: disposable pieces that DON'T help their cats, capped `_MEGA_DEPTH_WIDTH`=4 — they ride along to balance value / shed my own dead weight). "Disposable" (`_mega_disposable`) = within `_give_ceil` (or a sell-high I'd move anyway) AND one of: sub-star (`_tval < _STAR_TVAL_FLOOR`, which also catches a **weak incumbent** I'm replacing — e.g. Will Smith/Alvarez at C), a good bat at a **surplus hitter position**, or a good arm off a **surplus pitching ROLE** (`_PITCH_SURPLUS_CATS` — any pitching cat among my surpluses, the reliever/starter analog of a surplus position). **A plain top-N-by-score give pool silently broke this** — for a rival who needs PITCHING, my best-scored disposable bats never help them, so every package failed `scov` and NOTHING generated; guaranteeing the helper core fixes it.
      - **GET pool** = a **need-filler core** (`mega_need`: rival pieces that fill my cat need `get_cats` or a thin position, ranked by VALUE, capped `_MEGA_GET_WIDTH`=6 — wider than base `_TRADE_POOL_WIDTH` because a blockbuster reaches for a scarce C AND SS off the same rival) **+ VALUE ballast** (`mega_ballast`: rival mid-value non-need pieces in `[_MEGA_BALLAST_MIN, _MEGA_BALLAST_MAX]`=[0.30,1.20], capped `_MEGA_BALLAST_WIDTH`=3 — the Gausman piece: not an upgrade, just enough value coming back that giving the extra body isn't an overpay). Every package must fill **≥ `_MEGA_CORE_MIN`=2 real needs with actual need-fillers** (ballast can only ever be the 3rd/4th piece that balances, never the point of the deal) AND clear the ≥2-distinct-needs pre-filter.
    - My studs stay out (the `_give_ceil` + `_STAR_TVAL_FLOOR` gates), I can't be gutted (the both-sides `_leaves_position_short` HARD VETO in `_grade_package`), and a value-losing give still can't pass (the win-win `net_me` gate). Mega packages are graded by `_grade_package`, then kept **WIN-WIN ONLY regardless of `realistic_only`** (`my_verdict == "ACCEPT"` AND `net_them ≥ _TRADE_RIVAL_GAIN_MIN` AND `not _deal_star_reach`), and **ranked breadth-first with a SIZE REWARD + a SHED REWARD** (`score = |needs filled| + 2·net_me + 1·net_them + 4·timing + _MEGA_SIZE_BONUS·pkg_size + _MEGA_SHED_BONUS·shed`; `_MEGA_SIZE_BONUS`=0.9 so a genuine two-sided win that is ALSO bigger (4-for-4 / 4-for-3) beats a lean 3-for-2; `_MEGA_SHED_BONUS`=0.6 per give-piece that dumps my weak incumbent at a slot I'm upgrading — addition-by-subtraction, always safe since the depth floor guarantees a body comes back). Tagged `mega=True`.
    - **`find_megadeals(..., limit)`** is the wrapper: runs the mega-only path on the FAIR give-ceiling league-wide from `my_team`'s POV, then keeps **ONE PER PARTNER** (score-sorted, so the first per team is that team's best) before returning the top `limit`. **It temporarily lifts find_trades' return caps** (`_set_trade_caps(_MEGA_SCAN_CARDS=48, _MEGA_SCAN_PER_TEAM=2)` in a try/finally) — otherwise find_trades' default 6-card / 2-per-team cap truncates the cross-team pool BEFORE the one-per-partner dedup can pick variety (partner A's deals fill the 6 slots and partner B's best is lost). So the Trade Lab strip (`limit=2`) reliably shows two DIFFERENT teams; `[]` on many days is still expected. **Perf:** the wider pools cost ~15s across all 12 POVs for the Lab board (an offline build), ~0.1–2s for the digest's single POV. Consumed by the **digest Trade Radar** (`limit=1`, appended after 3 normal cards as the headline: solid PURPLE→MAGENTA "💫 Blockbuster" chip, purple card highlight, dynamic "why it's a blockbuster" callout) and the **Trade Lab 💫 Blockbuster strip** (`limit=2`, same purple highlight + `spark` line). **User-facing label everywhere is "Blockbuster"**; the internal name stays "consolidation megadeal". NOT on the dashboard (its zero-scroll tile can't fit a 3–4-player card).
  - **ROSTER-REDUNDANCY GUARD (`_non_redundant_get_pos` + `_team_position_counts` + `_POS_DEPTH_SLACK`=1):** an incoming hitter's positional-need credit is **stripped when the deal would leave me over-depth at that slot** — more eligible bodies than `POS_STARTERS[P] + _POS_DEPTH_SLACK` — **UNLESS I shed a body eligible there in the same deal** (a swap/upgrade still counts, `shed>0`). `_team_position_counts(hitters, team_key)` (formerly `_my_position_counts`, now team-general) counts a team's rostered YEAR bats per `_trade_pos_groups` label (multi-eligible bats count at each slot; **EXCLUDES IL-slot players** (`_on_il`) — a body on the IL isn't PLAYABLE now, so it can't fill a startable slot. Counting it made a 1-active-plus-1-IL catcher read as C-deep, wrongly suppressing C upgrades and letting "give your only active C" pass the depth floor; the IL body still returns eventually, but for near-term playability it doesn't count). Applied everywhere positional need is credited: `find_trades` (the 2-for-2 `benefits` gate AND final `gpos`), `_grade_pending_trades` (`gpos`), and Trade Lab (`posStacked` JS mirror). Suppresses the *positional* credit only; a genuine category need the same player fills still counts.
  - **BOTH-PARTIES DEPTH FLOOR (`_leaves_position_short` + `_TRADE_THIN_POS_PENALTY`=0.15) — the fix for "their only catcher for 2 OF":** a body-count floor INDEPENDENT of `_tval` (catcher category value collapses to ~0, so two productive OF out-value a lone C and the value math alone reads it "realistic"). `_leaves_position_short(team_counts, give_players, get_players)` returns the **hitter** positions where GIVING `give_players` and RECEIVING `get_players` drops a team below `POS_STARTERS[P]` bodies with no same-position body back (SP/RP excluded — pitching need is category-shaped). **HARD VETO in `find_trades`:** a package is skipped when `_leaves_position_short(t_pos_count, ins, outs)` (rival left short) OR `_leaves_position_short(my_pos_count, outs, ins)` (my side). **HONEST READ:** a surviving deal that leaves the RIVAL at exactly the floor at a single-slot position (`thin_them`) subtracts `_TRADE_THIN_POS_PENALTY` per slot from `net_them` (so `_trade_tilt` flips to "aggressive ask") and stashes `thin_note` ("leaves them without a backup at C") on the trade dict → rendered ORANGE in the digest + dashboard cards. **Pending offers (`_grade_pending_trades`) aren't vetoed** (they exist) — instead `_leaves_position_short(my_pos_count, outs, ins)` feeds `_pending_verdict(..., leaves_me_short=…)`, downgrading an otherwise-ACCEPT to COUNTER ("leaves you thin at SS — get a replacement first"). **Trade Lab mirrors it in JS** (`leavesShort`/`thinPos` next to `posStacked`; per-team `pos_count` + `DATA.posStarters` + `DATA.tune.thinPosPenalty` baked): the live verdict downgrades on a my-side short, "Would they do it?" hard-balks on a partner short (+ thin note via a read-only `netThemRead`), "Would you do it?" balks on a my-side short. Value-display rows stay pure — the penalty is read-layer only.
- **Buy-low / sell-high timing (`_tsell`/`_tbuy`):** hitters via `_regression_flag` (xBA/xSLG-vs-actual), pitchers via `pitcher_regression_flag` (ERA-vs-xERA, de-biased — see the pitcher-badge gotcha). **Directional:** selling-high on the GIVE side and buying-low on the GET side are the arbitrage; the reverse (dealing a riser, acquiring a regressor) is a TRAP, penalized in ranking and chip-warned ORANGE. Timing chip tooltips are role-neutral ("results running ahead/behind his Statcast expected").
- **TWO LANES (`find_trades(..., mode)`) — both grade on the SAME scarcity-weighted `_tval`.** `mode="favor"` (default) tilts value to me; `mode="fair"` finds realistic, near-even deals a rival would ACCEPT (favor-me by construction can't surface a fair-priced scarce-position deal). Each trade is tagged `"lane"`. **`find_trades_combined`** runs both lanes and **interleaves them leading with FAIR**, deduping identical packages and keeping the per-team (`_TRADE_PER_TEAM_CAP`=2) + total (`_TRADE_MAX_CARDS`=6) caps joint across lanes. The digest `build_trade_radar` and dashboard `build_context` both call `find_trades_combined` (NOT `find_trades` directly).
  - **FAVOR lane gate (`_favor_me`):** I don't overpay (`give − get ≤ _TRADE_GIVE_SLACK`(0.15)) and may extract up to a steal ceiling (`get − give ≤ _TRADE_MAX_EDGE`(0.45)); give-side capped at `_TRADE_MAX_VAL`(1.35) (sell-high exempt). Ranking `score = Σ my-need-depth(gcov+gpos) + 0.3·Σ their-need-depth(scov) + 5.0·net_val + 4.0·timing − 0.5·(pkg−1)`.
  - **FAIR lane gate (`_fair`):** near-even — I keep at most `_TRADE_FAIR_BAND`(0.20) edge and may PAY UP at most `_TRADE_FAIR_PAYUP`(0.35) to land a scarce need-fill. Give-side ceiling RAISED to `_TRADE_FAIR_MAX_VAL`(1.90) (deal a good chip for fair return, but still protect a franchise anchor). Ranking `score = Σ my-need-depth + 0.5·Σ their-need-depth + 4.0·timing − 4.0·max(0,−net_val) − 0.5·(pkg−1)` — their benefit UP-weighted (0.5) and paying up PENALIZED, so closest-to-even need-fills rank first. Both gates: 2-for-2 scales the value slack by ×1.5; shapes 1-for-1 + 2-for-2 (top-`_TRADE_POOL_WIDTH`=4 each side; incomers cover ≥ 2 distinct needs). `net_val` = my `_tval` edge (`get − give`), `timing` = `sell_out + buy_in − buy_out − sell_in`.
  - **Acceptance read (`_trade_tilt(net_val, ins=None, outs=None, net_them=None)` → `(value_phrase, accept_phrase, color)`):** shared by the digest card header + the dashboard tile. `value_phrase` is my-POV (*you win the value / even value / you pay up*, base `_tval` ±0.1); `accept_phrase` is the RIVAL's-POV — **realistic** (GREEN) vs **aggressive ask** (YELLOW), the rival must not *clearly lose*. When `net_them` (their demand-side net) is supplied it drives the read (`net_them ≥ -_TRADE_REALISTIC_MAX`, 0.05); otherwise a base proxy (`net_val ≤ _TRADE_REALISTIC_MAX`). **`_TRADE_REALISTIC_MAX` (0.05) is a dedicated read band, DISTINCT from `_TRADE_FAIR_BAND` (0.20)** (the latter stays the fair-LANE generation gate). `find_trades` computes `net_them` per deal; **all three callers** (`build_trade_radar`, dashboard `render_trade_radar`, Trade Lab's `build_partner_fit`) pass `net_them=t.get("net_them")`.
    - **GRADUATED star premium (`_star_premium(tval)` → `_deal_star_reach` / `_deal_star_surrender`)** — the endowment premium a manager attaches to a prized player, keyed on **trade VALUE `_tval`, NOT role score** (see NOTES, "star premium moved to `_tval`"). Convex: `max(0, min(_STAR_PREM_CAP=1.10, (tval − _STAR_TVAL_FLOOR=1.10) × _STAR_TVAL_SLOPE=1.4))` → 0.88→0.00, 1.10→0.00, 1.42→0.45, 1.52→0.59, 1.90→1.10. **Why value not score:** role score and `_tval` diverge structurally (ρ≈0.82) — a vulture-win reliever (Aaron Ashby: score 95, `_tval` 0.88) *scores* like a star but *trades* like a role player, and an elite closer scores like every other closer while trading far cheaper. Pricing the premium on the cross-role value currency fixes both and makes relievers comparable to hitters/SPs on ONE axis, so **the old `_star_role` reliever exclusion is GONE** — a mid reliever simply sits below the floor at ~0, an elite closer (Mason Miller, `_tval` 1.42 → 0.45) earns real premium.
      - **Premium basis is `_tval_star`, not `_tval`** (see the INJURY DISCOUNT bullet): healthy value for a short-term-injured player (still a star), discounted value only for a severe/season-lost tier. So an injured superstar keeps his reach premium unless he's out for the season.
      - **`_deal_star_reach(ins, outs, net_val)`** (rival balks): premium is **SUMMED across the package**, not max-per-side — `req = max(0, Σ_star_premium(ins._tval_star) − Σ_star_premium(outs._tval_star))` (rival surrenders `ins` = what I acquire, receives `outs` = what I give). Reach (→ **aggressive ask**) when `req > 0 and net_val > -req`. **Summing is what catches "two franchise players for one star + a role player"** — a lone high-value return can't mask that the rival ships two premium assets (the Miller+Witt-for-Ashby+McCarthy bug: max-per-side let McCarthy's high value + Ashby's inflated role score cancel the guard).
      - **`_deal_star_surrender(ins, outs, net_val)`** (MY-side mirror): `req = max(0, Σ_star_premium(outs._tval_star) − Σ_star_premium(ins._tval_star))`; fires when `req > 0 and net_val < req`. Wired into `_pending_verdict` (`star_surrender` param) — downgrades an incoming ACCEPT → **COUNTER** ("they're prying your star at par — hold out"); `_counter_suggestion` returns "".
      - **ACCEPTANCE-LAYER only, NOT value:** `_tval` stays a pure category-value currency; the premium is a separate acceptance-layer read on top of it. Star checks run on **base `net_val`** (not the demand-side nets). **Mirrored byte-for-byte into the Trade Lab JS** (`starPremium(tval)`/`dealStarReach`/`dealStarSurrender` — sum, no role filter; constants `starTvalFloor`/`starTvalSlope`/`starPremCap` baked into `DATA.tune`) driving the Lab's "Would they do it?" / "Would you do it?" reads.
    - **DEMAND-SIDE team-need value (`_need_mult` → net_me / net_them)** — rationale → NOTES. `_need_mult(row, need_cats, surplus_cats, need_pos=None, surplus_pos=None)` → a multiplier (start 1.0; `+_NEED_MULT_CAT`=0.14 per need cat covered, `+_NEED_MULT_POS`=0.20 for a hitter at a thin position, `−_NEED_MULT_SURPLUS`=0.10 when he only helps where they're deep; clamped `_NEED_MULT_CLAMP`=(0.70, 1.60)). `effective_value = _tval × _need_mult`. **Acceptance-read layer only** — NOT folded back into `_tval` or the `find_trades` ranking (no double-count). Used two ways: (a) `find_trades` computes **`net_them`** (drives the digest/dashboard tilt); (b) `_grade_pending_trades` computes **`net_me`** (my cat+positional net) and passes it to `_pending_verdict`. The displayed value phrase + counter-gap stay on BASE `_tval`. **Trade Lab shows BOTH POVs** via a three-row Base / My value / Their value block (client `needMult`/`sumEff` mirror `_need_mult`).
      - **`net_them` includes the RIVAL's positional need too, symmetric with the my-side calc (`_pos_need_surplus(pos_data, n)` — shared helper, factored out of the my-side inline block so both sides use one convention).** `find_trades` builds `t_need_pos`/`t_surplus_pos` per rival inside the team loop from `pos_data_by_team.get(team)` if a caller supplied that cache, else computes `positional_breakdown(pitchers, hitters, team, ...)` on demand — then passes them into the `their_get_val`/`their_give_val` `_need_mult` calls exactly like the my-side `my_get_val`/`my_give_val` calls already did. This closes a real divergence from the Trade Lab JS mirror (`netThem`/`partnerMeta.need_pos`), which always included the positional term — the digest was the one that had been skipping it. `find_trades(..., pos_data_by_team=None)` / `find_trades_combined(..., pos_data_by_team=None)`: `trade_lab.py`'s `build_partner_fit` passes its already-built `pos_data_by_team` (trade_lab.py — see below) to avoid a `positional_breakdown` call storm across its every-team-POV loop; the digest/dashboard callers omit it (cheap enough to compute per-rival on demand once per run).
- **Card render:** partner `fantasy_logo` + name; two columns **You give** (RED header, `send_cats` chips MUTED) / **You get** (GREEN header, `get_cats` chips ACCENT + **MAGENTA position chip** for a thin slot the incoming player upgrades &mdash; its own hue, so it can't be mistaken for the cyan QS season-skill badge on an incoming SP). `_trade_player_line(r, hi_cats, hi_color, side, show_pos, best_recent_p, best_recent_h, hit_pctile)` renders the **CANONICAL** buy/sell chip — the same glyph-only `$` (GREEN, `_tbuy`) / `▼` (RED, `_tsell`) used everywhere else. `side` ('give'/'get') only tunes the hover tooltip. **Tactical skill badges (`_trade_skill_badges`, right after the IL chip):** the SAME durable badges the Trade Lab shows — **PWR/SB** for hitters (`hitter_badges(..., regression=False)`) and **QS/K+** (season skill) **+ ⚠** (blowup floor) for starters (`sp_skill_badges + blowup_badge`) — so every trade surface (Trade Radar, Pending Trades, dashboard tile, Trade Lab) speaks one badge language. The `$`/`▼` is passed `regression=False` there because the card already renders its own side-aware directional version (above); RP carry no season-skill badge (SP-only). The SP breakdown panel appends `_sp_skill_context` so the QS/K+ chips get a tap-to-expand "why" (hitters already get theirs from `_hitter_score_breakdown`'s `_hit_badge_context`). **Every player's Score pill is tap-to-expand** (both give AND get sides): because trade players are stacked `<div>`s inside table cells, it uses `_trade_score_reveal` — a div-based analog of `score_reveal` (hidden `<div class="scorebd-div">` revealed by a `div.scorebd-div:target` rule; Gmail strips `<style>`). Breakdown fn role-picked off `_tptype`. Footer "Upgrades your {cats + `{pos} slot`} ; they shore up {send} · {value}" where value from `net_val`, plus a `· sell-high`/`· buy-low` thesis tag. Constants are one-line tunables near the FA-cats constants. **"Build in Trade Lab" link** (`_tradelab_button`, right side of the card's flex header row — no extra row): a plain link (no JS) into the hosted pocket Trade Lab (`TRADE_LAB_URL`) with the deal preloaded via the same `#partner=&give=&get=` hash `trade_lab.py`'s `preloadFromHash()` already parses — `partner=t["team"]`, `give`/`get` from `PlayerName` on `t["outs"]`/`t["ins"]`. `""` when both lists are empty. Styled as a quiet ACCENT text link (no button chrome), matching the digest's other muted inline links (e.g. the `↑ TOP` band link) rather than standing out as a CTA. **Net-value hint** (`_trade_net_summary`, same header row, immediately left of the link): a single quiet line — `base +N.NN · you +N.NN · them +N.NN` — off `net_val`/`my_get_val−my_give_val`/`their_get_val−their_give_val` (all already stored on the trade dict by `find_trades`). Plain numbers only, no verdict language — Trade Radar deals are always something *I'd propose*, so an accept/counter/decline framing doesn't apply (you can't "counter" your own offer); instead a bad-for-me deal never generates in the first place: `find_trades` hard-filters out any package where `_deal_star_surrender(ins, outs, net_val)` fires (would require surrendering my best-role star at even/negative value) via `continue` right after `net_val` is computed, so Trade Radar never suggests it to begin with.

### Pending Trades evaluator (`_grade_pending_trades` + `build_pending_trades_section`) — grades REAL offers
Grades the actual pending trade **offers** involving my team (snapshot `pending_trades`), reusing the Trade Radar machinery so every number matches the digest. Because offers **expire**, the incoming-offer headline surfaces HIGH (Week at a Glance + the Briefing "Act today" list), not just in the dedicated section. Gated on `not override_team`.
- **Grade ONCE, share everywhere:** `_grade_pending_trades(pending, pitchers, hitters, roto, my_team, best_recent_p, best_recent_h, pos_data, hit_pctile, pit_pctile, today_str)` → a list of graded dicts, computed once in `build_email` right after `pos_data`. The dedicated section, the Briefing, and the Week-at-a-Glance headline all read from it (`incoming_pending = [g for g in graded_pending if g["incoming"]]`) so they can't disagree.
- **Reuse (single source):** `_trade_value(r, ptype, hit_pctile, pit_pctile)` and `_enrich_trade_player(r, ptype, best_recent_p, best_recent_h, hit_pctile, pit_pctile)` (sets `_tscore`/`_tcats`/`_tval`/`_tsell`/`_tbuy`/`_tgroups`/`_tptype`) were **promoted from `find_trades` closures to module level** so both share one implementation. `find_trades` calls `_enrich_trade_player` via a thin local `enrich`. `_hitter_fills_need_pos(r, need_pos)` is the shared positional-need test.
- **Player→row resolution:** each traded player's ESPN name is matched to its snapshot YEAR row via a `_badge_name_key` lookup (fall back 30→15→7); role decided by which pool matches. Unmatched names render a plain "(no data)" line and are excluded from the value math (never crash).
- **Grade:** `net_val = Σ_tval(get) − Σ_tval(give)` (BASE value, drives the displayed value tilt + counter-gap, ±0.1 thresholds). A **demand-side `net_me`** = incoming valued by my needs − outgoing (`_need_mult`) drives the **verdict** so it's roster-aware. Category/positional impact = incoming `_tcats ∩ my_needs` + thin hitter positions filled; timing legs `sell_out+buy_in−buy_out−sell_in`.
- **Verdict (`_pending_verdict`, INCOMING only):** keyed off `net_me` (not base `net_val`) — ACCEPT (GREEN) when I win value (or roughly even AND it fills a need) and timing isn't a net trap; COUNTER (YELLOW) when the direction is right but I pay up or the timing is a trap; DECLINE (RED) when no need is addressed and I don't gain value. Rendered as a `_verdict_pill` + one-line rationale. **A `star_surrender` flag (`_deal_star_surrender`) downgrades any otherwise-ACCEPT to COUNTER** when the offer pries my crown-jewel star at par; **a `leaves_me_short` flag (`_leaves_position_short(my_pos_count, outs, ins)`) likewise downgrades ACCEPT→COUNTER** when accepting would strip me below startable depth at a hitter slot ("leaves you thin at SS — get a replacement first"). **OUTGOING** offers (I proposed) show an "Awaiting {partner} · {value} from your side" status instead.
- **Counter suggestion (`_counter_suggestion`, on a COUNTER):** when I'm overpaying by `gap` (= `−net_val`), suggests the single best ADD-ON to request from the partner — a spareable piece (`_tval ≤ gap + _TRADE_MAX_EDGE`) that closes the value gap (`_tval ≥ 0.7·gap`) and ideally helps one of my need cats/positions (preferring buy-low). Rendered as a 💡 line in the card **and** appended to the headline. Empty when no realistic add-on exists.
- **Expiry (`_fmt_trade_expiry`):** "expires in Nd" from the ISO `expires` field, shown RED in the card header, the headline, and the Briefing.
- **High-up headline (`_pending_headline(g, brief=False)`):** one-line 🤝 headline (partner · get/give · verdict pill · expiry · counter) prepended to **Week at a Glance** (`build_week_overview(..., trade_bullets=[...])`, top of the list) and the **Briefing** "⚡ Act today" list (`render_briefing(..., pending_incoming=incoming_pending)`, first items — they expire).
- **Card render (`build_pending_trades_section(graded, best_recent_p, best_recent_h, hit_pctile, team_logos)`):** reuses `_trade_player_line` verbatim, same You give (RED) / You get (GREEN) columns as Trade Radar, at the TOP of the TRANSACTIONS band (section 10b). Returns `""` when nothing is pending. Same **"Build in Trade Lab" button** as Trade Radar (`_tradelab_button(partner, g["give_names"], g["get_names"])`) on every card, incoming or outgoing.
